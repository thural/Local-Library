extends layout

block content
  h1 #{title}
  p #{book_instance.id}

    p Do you really want to delete this book copy?

    form(method='POST' action='')
      div.form-group
        input#bookinstanceid.form-control(type='hidden', name='instanceid', required='true', value=book_instance._id )

      button.btn.btn-primary(type='submit') Delete
